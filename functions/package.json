{
  "name": "fitdesi-functions",
  "version": "1.0.0",
  "description": "FitDesi Firebase Cloud Functions",
  "main": "src/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "test": "jest --testEnvironment node --coverage --coverageThreshold='{\"global\":{\"statements\":70}}'"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^5.0.0"
  },
  "private": true,
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
